# APK(안드로이드 설치파일) 만드는 방법

이 앱을 `.apk` 파일로 만들어 기사님들께 직접 전달할 수 있습니다.
카카오톡으로 파일을 보내면 기사님이 눌러서 설치하는 방식입니다.

준비 작업(아이콘·매니페스트)은 이미 다 해뒀습니다. 아래 순서만 따라하시면 됩니다.

---

## 1단계. PWABuilder에서 APK 만들기 (약 5분)

1. https://www.pwabuilder.com 접속
2. 주소 입력칸에 아래 주소를 넣고 **Start** 클릭
   ```
   https://jooho1679-lgtm.github.io/
   ```
3. 분석이 끝나면 **Package for stores** 버튼 클릭
4. **Android** 항목에서 **Generate Package** 클릭
5. 옵션 화면이 나오면 아래만 확인하고 나머지는 기본값 그대로 둡니다
   - **Package ID**: `io.github.jooho1679_lgtm.geongik` (기본값 그대로 둬도 됩니다)
   - **App name**: 경익운수 배차 알림
   - **Signing key**: **Create new** (새로 만들기) 선택
6. **Download** 를 누르면 zip 파일이 받아집니다

zip 안에 들어있는 것:

| 파일 | 용도 |
|---|---|
| `app-release-signed.apk` | **기사님들께 보낼 설치 파일** |
| `signing.keystore` | 서명키 — **절대 삭제·유출 금지** (아래 설명 참고) |
| `signing-key-info.txt` | 키 비밀번호 등이 적힌 파일 — 같이 잘 보관 |
| `assetlinks.json` | 2단계에서 사용 |

> **서명키(`signing.keystore`)는 반드시 안전한 곳에 보관하세요.**
> 나중에 앱을 새 버전으로 업데이트하려면 같은 키로 서명해야 합니다.
> 키를 잃어버리면 기존 앱을 덮어쓰는 업데이트가 불가능해집니다.
> (다만 아래 "업데이트" 항목처럼 시간표 변경은 APK 재배포 없이 자동 반영됩니다)

---

## 2단계. 주소창 숨기기 (assetlinks.json 등록)

이 과정을 **건너뛰면 앱 위쪽에 인터넷 주소창이 보입니다.** 등록하면 완전한 앱처럼 보입니다.

1. 받은 zip 안의 `assetlinks.json` 파일을 엽니다
2. 이 폴더(`경익운수배차알림`) 안에 `.well-known` 이라는 폴더를 만들고, 그 안에 `assetlinks.json` 을 넣습니다
   ```
   경익운수배차알림/
     └ .well-known/
         └ assetlinks.json
   ```
3. 터미널에서 아래 실행
   ```bash
   git add . ; git commit -m "assetlinks 등록" ; git push
   ```

> 파일 내용만 저에게 알려주시면 제가 대신 넣고 올려드릴 수도 있습니다.

등록 후 1~2분 뒤 아래 주소가 열리면 정상입니다:
https://jooho1679-lgtm.github.io/.well-known/assetlinks.json

---

## 3단계. 기사님들께 배포

1. `app-release-signed.apk` 파일을 카카오톡·문자 등으로 전송
2. 기사님이 파일을 누르면 설치 화면이 뜹니다
3. 이때 **"출처를 알 수 없는 앱"** 경고가 나옵니다 (스토어를 거치지 않은 앱은 모두 그렇습니다)
   - **설정 열기** → **이 출처 허용** 켜기 → 뒤로 → **설치**

---

## 시간표가 바뀌면? (중요)

**APK를 다시 만들 필요가 없습니다.**

이 APK는 웹사이트를 그대로 띄우는 방식이라, 제가 시간표를 갱신해서 올리면
기사님들 앱에 **자동으로 반영**됩니다. 앱을 다시 배포하지 않아도 됩니다.

APK를 다시 만들어야 하는 경우는 앱 이름이나 아이콘을 바꿀 때 정도입니다.

---

## 알아두실 점

- APK로 만들어도 **속은 지금 웹앱과 완전히 동일**합니다. 화면·기능·알람 모두 같습니다.
- 따라서 **알람이 화면이 꺼지면 안 울릴 수 있는 제약도 그대로**입니다.
  (APK로 바꾼다고 이 부분이 해결되지는 않습니다)
- 달라지는 건 **설치 경험**입니다: 홈 화면에 추가하는 안내가 필요 없고,
  앱 서랍에 정식으로 등록되며, 파일 하나만 보내면 됩니다.

## 구글 플레이 스토어에 올리려면

- 구글 개발자 계정 등록비 **$25(1회)** 필요
- 심사 기간 며칠 소요
- 개인정보처리방침 URL 등 서류 필요

사내 기사님들만 쓰는 앱이라면 **스토어 등록 없이 APK 파일 전달로 충분**합니다.
